[phases.setup]
nixPkgs = ["python311"]
[phases.install]
commands = ["pip install -r backend/requirements.txt", "pip install scrapling[fetchers]"]
[phases.build]
commands = ["echo 'No build step required'"]
[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
